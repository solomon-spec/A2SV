def test():  
    t = int(input())
    nums = list(map(int,input().split()))
    div = nums[0]%2
    for i in nums:
        if i%2 != div:
            nums.sort()
            return nums
    
    return nums

answer = list(map(str,test()))
print(" ".join(answer))
